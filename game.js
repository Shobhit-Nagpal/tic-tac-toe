//Module to check gameboard
const gameBoard = (() => {
    let gameboard = [];
})();

//Module to check game flow
const gameFlow = (() => {

})();

const players = (name, symbol) => {
    return {name, symbol};
}